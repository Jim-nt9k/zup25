Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
	КраткийСоставДокумента = ПроцедурыУправленияПерсоналом.ЗаполнитьКраткийСоставДокумента(СотрудникФизЛицо);
	//КраткийСоставДокумента = "";
	//Для Каждого СтрокаТЧ Из СотрудникФизЛицо Цикл
	//	Если СтрДлина(КраткийСоставДокумента) < 100 Тогда
	//		ФИО = ОбщегоНазначения.РазложитьСтрокуВМассивПодстрок(СокрЛП(СтрокаТЧ.СотрудникТекст)," ");
	//		
	//		КраткийСоставДокумента = КраткийСоставДокумента + ?(ЗначениеЗаполнено(КраткийСоставДокумента),", ","") + СокрЛП(ФИО[0]) + " " + Лев(ФИО[1],1) + "." + Лев(ФИО[2],1) + ".";
	//	Иначе
	//		КраткийСоставДокумента = Сред(КраткийСоставДокумента,1,100) + "...";
	//		Прервать;
	//	КонецЕсли;
	//КонецЦикла;
КонецПроцедуры
